import { type RemixConfig } from "../config";
export declare let serve: (initialConfig: RemixConfig, options: {
    command?: string;
    scheme: string;
    host: string;
    port: number;
    restart: boolean;
    tlsKey?: string;
    tlsCert?: string;
}) => Promise<unknown>;
