import { Navbar } from "@/components/navbar"

const Catalog = () => {
    return (
        <div>
            <Navbar/>
            Catálogo
            (Searchbar component)
            (Catalog component)
        </div>
    )
}

export default Catalog